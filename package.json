{
  "name": "event-circle",
  "version": "1.0.0",
  "description": "EventCircle - Event Management Platform",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "build": "cd frontend && npm run build"
  },
  "keywords": ["event", "management", "platform"],
  "author": "",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/username/event-circle"
  },
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "webpack": "^5.98.0",
    "webpack-cli": "^6.0.1"
  },
  "dependencies": {
    "fluent-ffmpeg": "^2.1.3",
    "helmet": "^8.1.0"
  },
  "engines": {
    "node": "18"
  }
}
